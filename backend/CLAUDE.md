# Backend — Assistente Médico API

FastAPI + LangGraph + RAG (Chroma) + SSE. Código em `src/assistente_medico_api/`.

Ao tocar no grafo LangGraph (`src/assistente_medico_api/graph/`), siga as convenções de nós abaixo
(fonte única em `.cursor/rules/langgraph-node-conventions.mdc`):

@../.cursor/rules/langgraph-node-conventions.mdc
