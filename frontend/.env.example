# URL base da API quando o backend estiver disponível (opcional)
# Dica: se você estiver rodando Chroma separado em 8000, use 8001 pro FastAPI.
# VITE_API_BASE_URL=http://localhost:8001/api
# Ativar chat real (SSE) contra o FastAPI; demais páginas continuam em mock em memória
# VITE_CLINICAL_API_HTTP=true
